{
  "name": "skilly",
  "version": "0.0.0",
  "private": true,
  "description": "Self-hosted registry for governing Anthropic-style SKILL.md agent skills, anchored in Microsoft Entra ID.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}
