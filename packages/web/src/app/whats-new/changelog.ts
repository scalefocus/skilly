// Changelog shown on the /whats-new page — newest version first.
//
// MAINTENANCE (see CLAUDE.md "What's new / changelog"): add one entry here in the SAME commit as
// every APP_VERSION bump, derived from that commit's message, BEFORE committing & pushing. Keep it
// user-facing and one line per version. Dates are the commit date (UTC, YYYY-MM-DD).
export interface ChangelogEntry {
  version: string;
  date: string; // YYYY-MM-DD
  summary: string;
}

export const CHANGELOG: ChangelogEntry[] = [
  { version: "1.125.0", date: "2026-07-13", summary: "Fixed: two audit entries written at the same instant could be recorded with their order swapped, after which the audit log's “Verify integrity” check would falsely — and permanently — report a broken hash chain (the same race caused an intermittent CI test failure). New entries are now strictly ordered, the migration automatically repairs any log already affected, and real tampering is still detected exactly as before." },
  { version: "1.124.0", date: "2026-07-12", summary: "Security: install links now stop working the moment their owner is deactivated in Entra. The git gateway refuses any personal install token whose owning user isn't active — even when the deactivation arrived through a sync path that doesn't revoke tokens — so an ex-employee's copied install commands can't fetch skills anymore. To whoever tries, the refusal looks exactly like any invalid token; platform admins additionally see each attempt in the System log as a new “install token refused: owner inactive” event (the only 401 the log records). System installs for CI pipelines are unaffected, and re-enabled users simply mint a fresh install." },
  { version: "1.123.0", date: "2026-07-10", summary: "System message: the announcement can now be up to 100 characters (was 50), and the duration selector adds two longer options after 1d — 1w (one week) and 1m (a fixed 30-day span). Everything else is unchanged: saving still replaces the current message and restarts the countdown from now, and it still disappears on its own once the time elapses." },
  { version: "1.122.1", date: "2026-07-10", summary: "System message pill: fixed the header announcement pill on desktop so a long message truncates with an ellipsis (full text still on hover) instead of overflowing behind the messages/bell/theme-toggle buttons. On mobile the pill now drops onto its own full-width line below the search box — beneath the header buttons and search — and wraps to show the whole message (no hover on touch), instead of being squeezed onto the icon row." },
  { version: "1.122.0", date: "2026-07-10", summary: "Requested skills: a request's detail page now has a “Fulfil with an existing skill…” search box next to Propose a skill. Pick a matching skill and the button swaps to Propose an existing skill — one click immediately fulfils the request (no review needed, since the skill's already published), credits you the same as building one, and notifies the requester. Only org-visible skills are searchable here, so the link is always openable by whoever asked." },
  { version: "1.121.0", date: "2026-07-10", summary: "Administration → Currently online now shows the page each user was last seen on, between their name/email and the “active … ago” pill (e.g. “Skill: SEO Checklist”, “Administration”, “—” if not yet known). It updates automatically as people navigate the app." },
  { version: "1.120.0", date: "2026-07-10", summary: "Usage dashboard: a skill's Top installers and Top viewers lists (in its expanded drill-down) now collapse to the top 5 people when there are more, with a Show more / Show less button at the bottom of each list. Each list expands and collapses independently, and expanding reveals everyone already returned for that window (no extra loading)." },
  { version: "1.119.0", date: "2026-07-10", summary: "Platform admins can now post a short system-wide message: a new “System message” card in Administration lets you set up to 50 characters of text with an expiration of 1h/4h/8h/1d, shown as an accent-colored pill in every signed-in user's header between the search box and the messages button. Saving always replaces the current message and restarts the countdown from now; it disappears automatically once the time elapses, or you can clear it immediately. It's a separate, dedicated mechanism — it never appears in notifications, the messages menu, or email." },
  { version: "1.118.0", date: "2026-07-10", summary: "Security hardening: the web app's Content-Security-Policy is now nonce-based per request, dropping the old 'unsafe-inline' allowance for scripts so an injected inline script can no longer execute. Operators can choose the posture with a new CSP_MODE setting — enforce (the default), report-only (a no-block shakedown that reports violations to a new /api/csp-report endpoint), or off (revert to the previous policy) — and the SCIM/git worker now sends baseline nosniff / frame-deny / no-referrer headers on every response. No visible change to how the app looks or behaves." },
  { version: "1.117.1", date: "2026-07-10", summary: "Overview page: the “Featured skills” section moved up — it now sits directly below the stats row and above the “Installing is one command.” explainer (previously it appeared lower, just above “Recently published”). Nothing else about it changes: still shown only to signed-in users, visibility-filtered per viewer, newest-pinned-first, and hidden entirely when nothing is featured." },
  { version: "1.117.0", date: "2026-07-09", summary: "Featured skills: platform admins can now spotlight skills onto the home page. A new Spotlight / ✓ Spotlighted toggle on a skill's page (admins only) pins it to a new “Featured skills” section on the Overview, shown just above “Recently published”. The section is visibility-filtered per viewer (restricted skills never leak), lists only installable skills newest-pinned-first, and hides entirely when nothing is featured. A new Administration setting caps how many skills can be featured at once (default 10, range 1–50) — at the cap you're asked to remove one before spotlighting another. Archiving a skill, or yanking its last remaining version, automatically un-features it." },
  { version: "1.116.0", date: "2026-07-09", summary: "Notification emails are now human-readable instead of raw data. The subject reads “Skilly - <what happened>” (e.g. “Skilly - Direct message”, “Skilly - New version published”, “Skilly - Proposal accepted”) and the body is a plain sentence with a clickable link to the right place — no more event codes or JSON. Every notification type is covered (messages, proposals, published versions, upstream drift, official skills, fulfilled requests), the in-app inbox labels match the email subjects, and a direct-message email now links straight to that conversation." },
  { version: "1.115.2", date: "2026-07-08", summary: "Administration → Currently online: the activity-window toggle (5m/1h/8h/24h/30d) and its caption moved down to sit directly above the “Search online users” box, with the toggle right-aligned — mirroring the range toggle on the Active-users trend chart just above it." },
  { version: "1.115.1", date: "2026-07-07", summary: "Fixed: collapsed Administration cards were showing a thin sliver of their body content below the header. A collapsed card now shows only its title and the compact status summary — nothing else peeks out." },
  { version: "1.115.0", date: "2026-07-07", summary: "The Administration page is now a proper platform console: every card (not just Namespaces and Email notifications) collapses, starts collapsed, animates open/closed, and remembers its own open/closed state per browser — with a compact live value on each collapsed header (and the SCIM sync and email-status pills always visible so a broken sync or channel never hides). A new Expand all / Collapse all control at the top opens or closes everything at once, and the page header now reads “Run the platform.” instead of focusing only on namespaces." },
  { version: "1.114.1", date: "2026-07-07", summary: "Fixed: the leaderboard sorted install counts as text, so once counts had different digit lengths someone with 9 installs could rank above someone with 80 (and the “Installs leader” badge could crown the wrong person). Rankings — and the installs tie-breaker on the other three sorts — are numeric again." },
  { version: "1.114.0", date: "2026-07-07", summary: "Email notifications: skilly can now email you your notifications alongside the in-app inbox. A new profile toggle (on by default) lets anyone opt out of notification email entirely. Platform admins get a collapsible “Email notifications” card on the Administration page to connect a Microsoft 365 service mailbox (sign-in via Entra; sent through Outlook/Graph, with automatic token renewal and a status pill when delivery is down), author the HTML look of the emails in a rich-text editor around a required [SYSTEM MESSAGE] placeholder, send themselves a test email, and disconnect the account — all audited. Emails always carry the same links as the in-app notification plus a “Manage email notifications” footer; chat alerts email at most once per conversation until read; the env-configured SMTP channel keeps working as a plain-text fallback." },
  { version: "1.113.1", date: "2026-07-06", summary: "Pointer skills now go by one name everywhere: the Catalog's Source filter says “External” (was “Mirrored”), matching the external pill on skill cards and the detail page's “External source” panel, and the dashboard stat is now simply “External” (was “External / mirrored”)." },
  { version: "1.113.0", date: "2026-07-06", summary: "Proposing a new version can now change everything but the slug: the title, description, categories, tags, and tool/harness are all editable (they update the skill when the version is accepted — only visibility stays a separate management action), and the files are optional — “Keep current files” (the new default) reuses the latest stable version's files byte-for-byte for metadata-only re-versions, as long as at least one field actually changes. Reviewers see an explicit “Changes on accept” old → new diff plus a “Files: unchanged — reuses v…” note, and the file browser works for reused pointer mirrors too." },
  { version: "1.112.2", date: "2026-07-06", summary: "Fixed: on a skill's page, the install version dropdown (“Install latest” / “Install v…”) and the external-skill download-format dropdown (.skill / .tar.gz) now close when you click anywhere outside them or press Escape — previously they stayed open until you picked an option." },
  { version: "1.112.1", date: "2026-07-06", summary: "Administration → Currently online: the activity-window toggle now offers 5 minutes (still the default), 1, 8, or 24 hours, or 30 days — replacing the old 15/30-minute steps with a month-long window. Your pick is still remembered per admin in the browser." },
  { version: "1.112.0", date: "2026-07-03", summary: "External (Pointer) skills' Download button is now a format dropdown: choose .skill (the default — skilly re-packs the mirrored archive as a ready-to-import skill bundle) or .tar.gz (the mirror exactly as stored). Hosted skills keep downloading the original upload unchanged." },
  { version: "1.111.5", date: "2026-07-03", summary: "The review queue's per-row “Delete this proposal” control is now a small ✕ button — the same ghost ✕ used by other inline remove/clear controls (e.g. the admin user-picker) — instead of a trash icon." },
  { version: "1.111.4", date: "2026-07-03", summary: "The Catalog's sort toggle now shows an icon on every option: “↗ Popular” (and “◎ Relevance” while searching) join the existing “★ Top rated” and “↻ Latest”." },
  { version: "1.111.3", date: "2026-07-03", summary: "Fixed: on the Administration page, the “Delete User Info” user-search dropdowns (find a user to delete / replace maintainer) now close when you click anywhere outside them, instead of staying open until a result was picked or the text cleared." },
  { version: "1.111.2", date: "2026-07-03", summary: "The Administration page's namespace management (search, review filter, “Create a namespace”, and the per-namespace editors) now lives in a single collapsible card at the bottom of the page. It starts collapsed — the header shows the total namespace count — and remembers whether you left it open or closed." },
  { version: "1.111.1", date: "2026-07-03", summary: "Fixed: on the Administration page, the user-search result dropdowns in “Delete User Info” (find a user to delete / replace maintainer) were painting underneath the “Currently online” card instead of on top of it — search results are now fully visible above the cards below." },
  { version: "1.111.0", date: "2026-07-03", summary: "Administration → Currently online now lets each admin pick how generous “online” is: a toggle in the card header selects the activity window — 5 minutes (default), 15 or 30 minutes, or 1, 8, or 24 hours (the long windows effectively show who was active today). The choice is remembered per admin in the browser; the DAU/WAU/MAU counters and trend chart keep their fixed windows." },
  { version: "1.110.0", date: "2026-07-03", summary: "System installs: platform admins get a “System install” checkbox next to a skill's install controls that mints a platform-owned install for CI pipelines and org tools — not tied to any user, valid regardless of later visibility changes, and audited on mint, uninstall, and reactivate. A new Mine / System installs toggle on Installed skills lets any platform admin see and manage them (with a System install pill, who minted it, and the client IP); the usage drill-down shows their clones as a separate “System install” bucket, and they never earn leaderboard credit." },
  { version: "1.109.0", date: "2026-07-03", summary: "Top contributors now wear leader badges under their avatar, everywhere their photo appears — chat, the messages menu, the leaderboard, proposal and request pages, maintainer lists, admin pickers, and their own profile. Four badges match the leaderboard stats (Installs, Proposals, Fulfillment, and Watch leader), each in an all-time variant (with a little crown) and a last-30-days variant; ties share the badge, and hovering one names it, e.g. “Installs leader — all time”." },
  { version: "1.108.1", date: "2026-07-03", summary: "The account menu (bottom-left) and the topbar messages dropdown now open and close with a brief animation instead of appearing instantly; on mobile the messages panel slides up as a sheet." },
  { version: "1.108.0", date: "2026-07-03", summary: "Fixed: proposing a Pointer skill from the skills-hub.ai registry no longer pins the ref to “main” (a branch the registry doesn't have), which made every mirror attempt fail with a 404. For a skills-hub source the field is now “Pinned version”: it fills itself with the registry's latest version, autocompletes the published ones, and warns on a version that doesn't exist — and skilly rejects a branch-like ref at submission instead of letting the mirror dead-letter." },
  { version: "1.107.0", date: "2026-07-03", summary: "The leaderboard now has a fourth stat, “skills watched” — the number of skills a person maintains that someone else is watching — with a matching Watched rank-by option. Watching your own skill never counts toward it." },
  { version: "1.106.1", date: "2026-07-02", summary: "A fulfilled skill request's page now has a prominent “Open the skill →” button (in the main action row) that jumps straight to the skill that fulfilled it, instead of just a small link in the “Fulfilled by …” banner." },
  { version: "1.106.0", date: "2026-07-02", summary: "Platform admins can now filter the Requested skills page by request state — Open (default), Fulfilled, or All — beside the search bar, so they can review what's already been fulfilled, not just what's still open. The selector is admin-only (everyone else still sees the org-wide open list); fulfilled rows show a status pill." },
  { version: "1.105.0", date: "2026-07-02", summary: "Reviewers can now permanently delete a proposal from the review queue (or its detail page) — housekeeping for spam, duplicates, test submissions, or mistakes, distinct from rejecting it. It's silent (the submitter isn't notified) and audited. Accepted proposals can't be deleted (they're the record of a published version); the delete removes the proposal, its revisions, and its review discussion, while the audit trail is kept." },
  { version: "1.104.0", date: "2026-07-02", summary: "Fixed: proposing a skill for a request and publishing it directly (in a namespace where review is off) now fulfils the request too — previously only going through review-and-accept did. Requested skills gains a “Mine” toggle to see your own requests in any state (open or fulfilled, with a status pill), instead of just the org-wide open list." },
  { version: "1.103.0", date: "2026-07-02", summary: "Skill requests now have a Discussion card — any signed-in user can chat about a request (ask questions, offer to build it), not just the requester. The requester's own messages are tagged “Original Requester”. Discussions show up in the Messages window and send notifications just like a proposal's review discussion, and lock once the request is fulfilled." },
  { version: "1.102.0", date: "2026-07-02", summary: "Quick start now opens with “If you're new to the AI skill game” — a beginner-friendly section on installing Node.js and Python (with download links) and opening a command line on Windows, macOS, or Linux to verify the install, since skills run on your own machine. The same guidance is now in the User Manual (§5.2)." },
  { version: "1.101.0", date: "2026-07-02", summary: "Requested skills now gets the same “new to you” treatment as the Catalog: the sidebar item shows a 1–9+ count of open requests posted since your last visit, and each new-to-you request carries a “new” tag on its card or row until you next open the page. Editing an already-seen request doesn't bring the tag back." },
  { version: "1.100.2", date: "2026-07-02", summary: "Renamed the admin's \"Remove\" button on a skill request's page to \"Delete\", for clarity (it already permanently deletes the request)." },
  { version: "1.100.1", date: "2026-07-02", summary: "Withdrawing your own skill request now permanently deletes it (previously it was just hidden from the Requested skills list) — the confirmation now says so, and this can't be undone. Only the requester can withdraw their own request; admin removal is unchanged." },
  { version: "1.100.0", date: "2026-07-02", summary: "One-time cleanup: skill requests that were removed by an admin before removal became a permanent delete are now purged from the database, matching the current behavior." },
  { version: "1.99.1", date: "2026-07-02", summary: "Platform admins removing a skill request from its details page now permanently delete it, instead of just hiding it from the Requested skills list — the confirmation now says so, and this can't be undone. Withdrawing your own request is unchanged." },
  { version: "1.99.0", date: "2026-07-02", summary: "Skill requests (“I want a skill”) are now text-only: the optional example-files upload has been removed from the request form, along with the “Skill request example files” admin setting that controlled the file count and size limits. Existing requests and their pages are unaffected; only the file attachments and their download links are gone." },
  { version: "1.98.1", date: "2026-07-02", summary: "Posting a skill request now locks the form while it's being posted: pressing “Post request” dims the whole form so nothing can be edited mid-send, with the button showing “Working…”, and it stays locked while the new request page opens. The lock lifts if a similar-request warning appears (so you can read it and decide) or if there's an error. Editing any field after that warning now re-checks for similar requests before posting, instead of posting the changed request unchecked." },
  { version: "1.98.0", date: "2026-07-02", summary: "Administration gains a “Skill request example files” setting: platform admins now control how many example files an “I want a skill” request may carry (1–20 via a number spinner, default 5) and the largest allowed size of each file (same size steps as the upload limit, default 100 MB, per file). The request form shows the active limits and enforces them; lowering a limit never affects already-posted requests." },
  { version: "1.97.0", date: "2026-07-02", summary: "New “Request a skill”: on the Propose page, flip the new “I want a skill” toggle to post a request for a skill that doesn't exist yet (title, categories, description, usage, tool — plus optional example files for whoever builds it). Requests appear on a new “Requested skills” page (below Propose a skill); opening one offers a “Propose a skill” button that pre-fills the propose form, and when that proposal is accepted the request is fulfilled — it leaves the list and the requester is notified with a link to the new skill and who built it. The leaderboard gains a “requests fulfilled” stat and a rank-by toggle: Installs (default), Skills proposed, or Requests fulfilled. Similar-looking requests get a friendly heads-up before posting (you can post anyway), requesters can withdraw their open requests, and platform admins can remove any." },
  { version: "1.96.0", date: "2026-07-02", summary: "skilly is restyled to the Scalefocus brand: navy and cyan replace the previous warm palette, headings are now Montserrat with Open Sans body text, and status colors follow the brand's green/orange/red. Both light and dark themes carry the new identity, and skilly gets its own product mark — the wordmark's dot is now a cyan diamond, echoed in the new browser-tab icon. All user-manual and Quick start screenshots were re-captured in the new look." },
  { version: "1.95.1", date: "2026-07-01", summary: "Skill page download buttons (the top “Download” button and each version's download button) now explain why a Pointer skill downloads as .tar.gz, right in the tooltip." },
  { version: "1.95.0", date: "2026-07-01", summary: "Administration → Currently online now has an active-users trend chart above the DAU/WAU/MAU counters, with the same 7d/30d/90d/All windows as the Usage page. A background job records each day's active-user count once a day, so this is a real history (unlike the live DAU/WAU/MAU counters) — it starts with a short line right after this ships and grows day by day." },
  { version: "1.94.0", date: "2026-07-01", summary: "Administration → Currently online now shows Daily/Weekly/Monthly Active User counts (distinct users active in the trailing 24 hours, 7 days, and 30 days) right above the live online list, refreshed on the same cycle. These are live counts, not a historical trend." },
  { version: "1.93.1", date: "2026-07-01", summary: "Installed skills: clicking anywhere on a row (not just the title) now opens that skill's page — the Activate/Uninstall buttons and the reactivate-expiry controls still work exactly as before without triggering navigation." },
  { version: "1.93.0", date: "2026-07-01", summary: "“Skills you might like” now only suggests skills you can see and haven't installed yet (a skill you've installed or downloaded — even if later uninstalled — is dropped). If there were related skills but you already have them all, the section shows “You have all related skills.”; if a skill has no related skills you can see at all, the section stays hidden." },
  { version: "1.92.0", date: "2026-07-01", summary: "The Audit log and System log pages now have an “Export CSV” button (platform admins only) that downloads exactly what's currently filtered on screen — same search, action/status, and date range — capped at 50,000 rows newest-first. If the filtered results are larger than that, the download still succeeds with the most recent 50,000 and a notice tells you to narrow the date range for the rest." },
  { version: "1.91.0", date: "2026-07-01", summary: "Administration gains a “Maintenance” card where platform admins can rebuild the “Skills you might like” recommendation index on demand with a “Rebuild now” button — handy right after a burst of installs, instead of waiting for the nightly run. It shows when the index was last rebuilt and how many links it holds, and the action is audited." },
  { version: "1.90.0", date: "2026-07-01", summary: "Skill pages now end with a “Skills you might like” section — up to three other skills most often installed together with the one you're viewing (a co-install signal, refreshed nightly). It only shows skills you're allowed to see, and is hidden for new or rarely-installed skills that don't have enough signal yet." },
  { version: "1.89.2", date: "2026-07-01", summary: "The Overview page's “Installs this month” stat is now “Total installs” — an all-time count of every install across the platform's history, instead of just the current calendar month." },
  { version: "1.89.1", date: "2026-07-01", summary: "Skill detail page: the action buttons (Share, Watch, Download, New version, Mark/Unmark Official, Archive, …) now sit on their own row below the identity chips (@namespace · Official · version · visibility), instead of crowding onto the same line — so a skill with many controls stays readable on desktop." },
  { version: "1.89.0", date: "2026-07-01", summary: "New “Official” badge: platform admins can endorse first-party or sanctioned skills, so you can tell official from experimental at a glance. The badge shows on catalog cards, list rows, the search dropdown, and the skill page (with who marked it and when); a new “Official” catalog filter shows only endorsed skills, and Official acts as a gentle tiebreaker in the default sort. It's an endorsement, not a security claim — every skill is still scanned. Maintainers are notified when their skill is marked Official." },
  { version: "1.88.1", date: "2026-06-30", summary: "Quick start page on mobile: the closing buttons (Got it — go to the catalog, What's new, Installed skills) now each sit on their own full-width line instead of wrapping two-up. Desktop unchanged." },
  { version: "1.88.0", date: "2026-06-30", summary: "New Quick start page — a short, screenshot-driven tour of the essentials (find a skill, open it, install it, manage your installed skills, stay in the loop, and how to contribute). It opens automatically the first time you sign in, and is always available afterwards from the account menu, above What's new." },
  { version: "1.87.0", date: "2026-06-30", summary: "The review queue (Proposals → To review) is now ordered newest-first — the most recent submission sits on top — regardless of which state filters are applied, and it loads in batches of 100 with infinite scroll so it scales to any backlog. The state-filter chips and the To-review tab count now show the real totals across your whole review scope rather than just the rows scrolled into view." },
  { version: "1.86.12", date: "2026-06-30", summary: "Installed skills are now listed alphabetically by skill title (A→Z, case-insensitive) instead of by install date, so a skill is easier to find in a long list. Multiple installs of the same skill are ordered most-recent first." },
  { version: "1.86.11", date: "2026-06-30", summary: "Profile page on mobile: a long display name or email now stays on one line and fades out at the right edge of the profile card instead of overflowing it. The full value is available via the element's tooltip." },
  { version: "1.86.10", date: "2026-06-30", summary: "Usage page on mobile: a skill's tendency sparkline and the install/view stat strips now span the full width of the card — each on its own line, with the four time-window stats spread edge to edge — instead of sitting on the left with empty space to the right. Desktop unchanged." },
  { version: "1.86.9", date: "2026-06-30", summary: "Administration on mobile: in a namespace's role-mapping editor, the “Select Entra group” dropdown (and the member/admin role dropdown) now stay within the card instead of overflowing off the right edge when a group name is long — they shrink to fit and take their own full-width line on narrow screens. Desktop unchanged." },
  { version: "1.86.8", date: "2026-06-30", summary: "Administration → Delete User Info on mobile: a selected user's status tag now sits below their name and email with the clear ✕ moved to the top-right, and the Delete button drops onto its own full-width line below the “replace maintainer” field (the find-user and replace-user fields also each take their own line). Desktop layout is unchanged." },
  { version: "1.86.7", date: "2026-06-30", summary: "User-search result dropdowns now render on top of surrounding cards and sticky headers instead of being painted behind them — fixing the “add a maintainer” search on a skill's page, the “Delete User Info” pick/replace-user search in Administration, and the proposal tag suggestions." },
  { version: "1.86.6", date: "2026-06-30", summary: "Administration on mobile: in a namespace's role-mapping cards, a long group name or group id now stays on one line and fades out at the card's right edge instead of overflowing and being cut off. The full value is available via the element's tooltip." },
  { version: "1.86.5", date: "2026-06-30", summary: "Administration on mobile: the “Create a namespace” form now stacks — the slug and name boxes become equal full-width fields, and the “require review” toggle and Create button each drop to their own line. Role-mapping cards also restack on mobile: the group name, its id, and the role (admin/member) each sit on their own line, with the remove ✕ moved to the top-right. Desktop layout is unchanged." },
  { version: "1.86.4", date: "2026-06-30", summary: "Installed skills: actually pinned the Uninstall/Activate buttons to each row's top-right corner. The previous attempt still let them wrap to a second line because the row wraps (for the reactivate panel) and the title/details column was sized to its content; the column now starts from zero width so the buttons keep their corner at every screen size." },
  { version: "1.86.3", date: "2026-06-30", summary: "Installed skills: fixed the Uninstall/Activate buttons dropping to their own line at the bottom-left of a row on desktop instead of staying pinned to the card's top-right corner. The row's title and details now share one flexible column so the action buttons keep their corner regardless of how wide the details get (this had regressed when the install IP was added). Mobile was unaffected." },
  { version: "1.86.2", date: "2026-06-30", summary: "Catalog list view on mobile: a skill's rating and install count now drop onto their own full-width row at the bottom of each list item, instead of crowding in among the version and harness chips on narrow screens — matching the card view's bottom stats row. No change on desktop." },
  { version: "1.86.1", date: "2026-06-30", summary: "Catalog cards: a skill's rating and install count now sit on their own row at the bottom of the card, divided by a thin line from the harness and category chips above — so the popularity stats read consistently across cards instead of wrapping in among the chips." },
  { version: "1.86.0", date: "2026-06-30", summary: "Install counts are now per unique user, so the number can't be inflated by re-installing. A skill's install total counts each person at most once (their first install ever — re-clones for updates, and new versions, don't add to it), and a first download counts the same way and shares that tally. The contributor leaderboard follows suit: a maintainer earns at most one credit per unique adopter of their skill, and installing a skill you maintain yourself earns nothing. Activity charts and the monthly total still count every clone. Note: existing install totals were recomputed down to unique-user counts, so popularity numbers may drop." },
  { version: "1.85.0", date: "2026-06-30", summary: "Installed skills: each install now shows the client IP it was first made from (next to the git client and install date), so you can tell where a skill was pulled to. The IP is captured once on the first clone and is visible only to you on your own Installed page. For it to be the real client address rather than your reverse proxy, set the new TRUST_PROXY env var on the worker (see the deployment manual / .env.example); until then it shows nothing." },
  { version: "1.84.1", date: "2026-06-29", summary: "Skill detail page: the SKILL.md panel now starts collapsed to about the height of the Install box, with a fade at the bottom and a “Show more” button to expand the full contents (and “Show less” to re-collapse). Short SKILL.md files that already fit show in full with no button." },
  { version: "1.84.0", date: "2026-06-29", summary: "User avatars now appear for people who haven't signed in yet. Previously a profile photo was only captured when that person logged into skilly themselves, so freshly SCIM-synced users showed only initials — including in the maintainer search on a skill's page. The Entra reconciliation now also back-fills each synced user's profile photo from Microsoft Graph (where one exists and the app has photo-read permission), and the maintainer “add” search results now show the avatar bubble next to each match. Avatars a user set via their own sign-in are never overwritten." },
  { version: "1.83.2", date: "2026-06-29", summary: "Fixed the top-bar search box keeping its text (and popping the “See all results in catalog” dropdown) after you leave the catalog. Navigating to another page now clears the box; arriving on the catalog still seeds it from the active ?q= filter." },
  { version: "1.83.1", date: "2026-06-29", summary: "Administration → namespaces: the “Maintainer contact” field now has a user-search typeahead — start typing a name or email and pick a user to fill in their email — like the maintainer search on a skill's page. It's still free-text, so you can also enter a shared mailbox or distribution list." },
  { version: "1.83.0", date: "2026-06-29", summary: "Maximum upload size: added a 200 MB option (between 100 MB and 1 GB) and made 200 MB the new default — so the full set is 100 KB / 1 MB / 10 MB / 50 MB / 100 MB / 200 MB / 1 GB. The new default only applies where an admin hasn't set a specific limit; existing limits are unchanged." },
  { version: "1.82.2", date: "2026-06-29", summary: "The Proposals page “My submissions” tab now opens with no state filter selected — showing all your submissions across every state — instead of defaulting to “Changes requested”. (The To review tab still opens to the open states.)" },
  { version: "1.82.1", date: "2026-06-29", summary: "Leaderboard on mobile: the per-row “Skills” and “Reach out” buttons now drop onto their own full-width line below the rank, avatar, name, and installs, instead of crowding that row on narrow screens." },
  { version: "1.82.0", date: "2026-06-29", summary: "The leaderboard rows now have two actions. “Skills” opens the catalog filtered to the skills that person maintains (under a dismissible “Skills maintained by <name>” banner, and only skills you're allowed to see); on your own row it opens your “My Skills” view. “Reach out” starts a one-to-one chat with that person — hidden on your own row, since you can't message yourself." },
  { version: "1.81.0", date: "2026-06-29", summary: "The default maximum upload size for hosted-skill bundles is now 50 MB (was 10 MB), and the Administration size picker gains 50 MB and 1 GB options — so the full set is 100 KB / 1 MB / 10 MB / 50 MB / 100 MB / 1 GB. Crucially, the chosen limit is now honored at every stage (upload, publishing, pointer mirroring, scanning, and download/preview) — previously bundles over 10 MB were accepted at upload but then failed to publish because later steps had their own lower caps. The new default only applies where an admin hasn't set a specific limit; existing limits are unchanged. Note: very large uploads (100 MB / 1 GB) need ClamAV's stream limit and web/worker memory raised — see the deployment manual." },
  { version: "1.80.0", date: "2026-06-29", summary: "The Review queue nav badge is now a combined 1–9+ count of everything needing you on the Proposals page since you last looked: proposals awaiting your review (if you're a reviewer) plus your own proposals a reviewer sent back with “changes requested” (if you're a proposer). So a pure proposer gets a badge when a skill they proposed needs changes, and reviewers get re-badged when a proposer resubmits. It clears when you open the Proposals page. Also: the To review tab now opens with all three open states selected (Proposed, Under review, Changes requested) instead of only Proposed." },
  { version: "1.79.0", date: "2026-06-29", summary: "Administration now shows an “Identity sync (SCIM)” panel above the role-mapping editors: how many Entra groups and users have synced, and when groups last synced. Since the group → role pickers can only list groups Entra has provisioned over SCIM, an empty picker is no longer a mystery — when no groups are present the panel explains the likely cause, distinguishing “users are syncing but Group provisioning is off” (assign your groups to the Enterprise app and include groups in the provisioning scope) from “nothing has synced yet” (provisioning off, or wrong SCIM URL/token)." },
  { version: "1.78.0", date: "2026-06-29", summary: "Administration gains an “Install URL expiry” setting (platform admins): how far in the future a user may set an install command's expiry date, as a whole number of calendar months (1–120, default 12) — replacing the previous fixed one-year limit. It bounds the date picker when minting or extending an install and is enforced server-side; the “Never” option is unaffected, and lowering the limit only applies to newly minted install URLs, never ones already issued." },
  { version: "1.77.0", date: "2026-06-29", summary: "Platform admins now get a “Retry mirroring” button on a Pointer/external skill whose mirror failed after 5 attempts. Clicking it re-arms the mirror for 5 fresh attempts against the same source — handy after a transient upstream or network fault (a genuinely wrong ref or URL still needs a new version). The action is audited." },
  { version: "1.76.1", date: "2026-06-29", summary: "Fixed Pointer (external git) skills failing to publish with “Mirroring failed … server certificate verification failed (CAfile: none)”. The worker image was built without a CA certificate bundle (a side effect of a minimal install), so it couldn't verify HTTPS certificates when cloning external repos like github.com. CA certificates are now installed in both the worker and web images. Rebuild and redeploy the images to pick up the fix; affected skills can then be re-proposed (or will mirror on the next refresh)." },
  { version: "1.76.0", date: "2026-06-29", summary: "Proposals page: it now has two tabs — “My submissions” (everyone) and “To review” (reviewers). My submissions lists every skill you've proposed, filterable by state just like the review queue, so you can always find your own work. And when a reviewer requests changes, you can now revise the proposal in place and resubmit it: edit the description, categories, usage and — for a brand-new skill — the title, tool/harness and visibility, change the version, and replace the files (upload a new bundle, or edit the pointer repo/ref/folder). Changed files and pointers are re-scanned and re-checked for duplicates, and resubmitting puts the proposal back in front of the reviewers automatically." },
  { version: "1.75.1", date: "2026-06-29", summary: "The catalog's “My Skills” filter now shows the skills you maintain (you're an explicit maintainer of), instead of the skills you originally proposed. Being a namespace admin doesn't auto-include every skill in that namespace — only skills you've been named a maintainer of." },
  { version: "1.75.0", date: "2026-06-29", summary: "The contributor leaderboard now credits a skill's installs to its current maintainers, not the original proposer. Each install counts once for every explicit maintainer of the skill at the moment it was installed — so if a skill has three maintainers, an install gives +1 to each of the three. Changing a skill's maintainers only affects future installs: people keep the credit they earned while they were maintainers, and newly added maintainers start earning from when they join (namespace admins aren't auto-credited — they have to be added as an explicit maintainer). Existing installs were backfilled to each skill's original proposer so the board stays continuous. When a user is deleted (Delete User Info), their leaderboard credit is removed and they no longer appear — the installs themselves still count for the skill's other maintainers." },
  { version: "1.74.1", date: "2026-06-29", summary: "Smart polling for chat got a small tweak: opening the messages menu (clicking the topbar messages button) now drops the conversation-list refresh straight back to the fastest interval (7s by default), so the moment you open chat it's at its most responsive. As before, the list eases off through the larger intervals while nothing's happening and holds at the slowest (53s by default) until a new message, a reply you send, or opening the menu brings it back to the floor." },
  { version: "1.74.0", date: "2026-06-28", summary: "Search now behaves differently in two places. The top-bar search dropdown shows the top 5 matches with a “See all results in catalog →” footer, and opens from 2 characters. On the Catalog page the dropdown is replaced by live filtering — typing narrows the cards/rows in place and re-orders them as you type, just like picking a category or tool, and clearing it restores the full list. Both surfaces now match the same way: substring matching across title, slug, description, tags and usage (so partial words match), with name matches shown first, then the most-installed. (Full-text relevance ranking is traded for this consistent, responsive matching.)" },
  { version: "1.73.0", date: "2026-06-28", summary: "In-app chat now uses “smart polling”. An open conversation refreshes at a floor interval (7s by default), and the message-list refresh starts there too, then eases off through larger intervals while nothing's happening — snapping straight back to the floor the moment a new message arrives or you send a reply. The cadence is a new Administration setting (“Chat refresh cadence”): a comma-separated set of seconds, defaulting to the primes 7, 11, 17, 19, 29, 41, 53 so the polls rarely line up with other requests. Changes take effect on each client's next page load. No WebSockets, so it works behind any corporate proxy." },
  { version: "1.72.1", date: "2026-06-19", summary: "When proposing a Pointer skill, the Pinned ref field now defaults to “main” (the usual default branch) instead of the proposed version — so the common case (a repo with a main branch and no version tag) works without editing. If the repo has no main, the form warns you to pick an existing branch or tag." },
  { version: "1.72.0", date: "2026-06-19", summary: "When you propose a new version of a skill, the Description is now editable (pre-filled with the skill's current description), alongside Categories. Accepting the version updates the skill's description to match — so you can revise a skill's description as part of a re-version, instead of it being read-only." },
  { version: "1.71.0", date: "2026-06-19", summary: "Proposing skills got two fixes. (1) Pointer skills: the pinned ref defaults to the version, but if that ref doesn't exist in the external repo, the form now auto-selects the repo's “main” branch when it has one — so you no longer get a dead-end “1.0.0 isn't a branch or tag” error on a repo that just uses main. You're only warned to pick a ref if “main” is also missing. (2) When you propose a new version of a skill, Categories are now editable (pre-filled with the skill's current ones) instead of read-only — accepting the version updates the skill's categories to match, so you can re-categorize a skill on a re-version." },
  { version: "1.70.0", date: "2026-06-19", summary: "Skill detail page: the Download button now gives you back the original file that was uploaded in the proposal, with its original extension — a skill uploaded as a .skill downloads as a .skill, a .zip as a .zip, a .tar.gz as a .tar.gz — instead of always re-packing to .zip. Each version's button shows the right extension. Also, your first download of a skill now counts toward its install total (just like an install), but is never listed among your Installed skills; repeat downloads don't double-count." },
  { version: "1.69.0", date: "2026-06-18", summary: "Administration → Delete User Info: the “find a user” and “replace maintainer” search results (and the selected user) now show the user's avatar and an Enabled/Disabled status chip alongside their name and email. And when a user is deleted, their messages and review comments now show their former email followed by “ - Deleted” (e.g. “alice@corp.com - Deleted”) instead of a generic “Deleted User”, so deleted authors stay identifiable in threads." },
  { version: "1.68.0", date: "2026-06-18", summary: "Skill maintainers: the maintainer list on a skill's page is now visible to anyone who can see the skill (not just admins), read-only, with a “Reach out” button to message a maintainer. Adding and removing maintainers is now open to a skill's own maintainers too — a platform admin, the namespace's admins, or any of the skill's maintainers can add or remove an explicit maintainer (previously a plain maintainer could only remove themselves). Everyone else just sees and reaches out." },
  { version: "1.67.0", date: "2026-06-18", summary: "Security: restored the full database guard on published skill versions. A regression had left a version's stored artifact location, external provenance (origin URL/subdir), and prerelease flag editable after publish — so a bug or compromised process could repoint a version's bytes or origin while keeping the recorded checksum. The guard now again pins the complete immutable content set on update (only yank/restore status and the publish flag may change), while keeping the platform-admin permanent-delete path intact." },
  { version: "1.66.0", date: "2026-06-17", summary: "A skill's detail page no longer offers an install command until its git repository is actually built. A just-published version is live immediately but its repo is synthesized by a background sweep (within ~a minute); installing before then would fail to clone. The page now shows a “Publishing…” state until the repo is ready, the version picker lists only installable versions, and the install endpoint refuses to mint a command for a not-yet-published version." },
  { version: "1.65.1", date: "2026-06-17", summary: "Catalog: the toolbar (Archived toggle, sort, view) now stays visible for admins/maintainers even when the default list is empty — so when every skill is archived you can still toggle Archived to see them, instead of being stuck on an empty state with no controls." },
  { version: "1.65.0", date: "2026-06-17", summary: "Fixed “No skills found” on install: skill repos are now synthesized with SKILL.md at the repository root, the layout `npx skills add` actually reads for a single-skill repo. A recent change had wrapped files in a `<slug>/` subdirectory — a location the tool doesn't scan — so freshly published skills cloned but showed no skill. Existing skills synthesized under the subdirectory need to be re-synthesized to pick up the root layout." },
  { version: "1.64.0", date: "2026-06-17", summary: "Hardened the git-repo self-heal so installs stop failing on partially-synthesized skills. The sweep now reconciles each published skill's repo against the database — re-creating any missing version tag (which caused pinned installs to fail with “Remote branch vX.Y.Z not found”) and repointing the default branch at the latest stable version (a missing/stale default branch made “latest” installs clone empty) — instead of only checking that the repo had any content at all." },
  { version: "1.63.0", date: "2026-06-17", summary: "Sign-in now self-heals a mis-provisioned identity: if SCIM stored a user keyed on the wrong Entra attribute (Entra's default maps SCIM externalId to mailNickname, not the objectId the login oid claim uses), the user could never log in. On sign-in, skilly now relinks the account matched by email to the real Entra objectId, so they get in. The correct fix is still to map externalId → objectId in Entra (deployment manual updated)." },
  { version: "1.62.0", date: "2026-06-17", summary: "SCIM DELETE /Users/:id (Entra removing a user) now performs the full GDPR erasure — the same as the admin “Delete User Info” flow but with no maintainer transfer — instead of just deactivating. A reversible disable (PATCH active:false) still just deactivates + revokes tokens. The erasure is idempotent and audit-logged." },
  { version: "1.61.0", date: "2026-06-17", summary: "Administration gains a “Delete User Info” section (platform admins only): search a user, optionally pick a “Replace maintainer to” user, and after a typed confirmation erase their personal info (GDPR). Their skills are kept; they're removed as a maintainer (their maintained skills transfer to the replacement where possible); their messages and review comments become “Deleted User”. The account is detached from Entra, so the person can return later as a fresh account." },
  { version: "1.60.1", date: "2026-06-17", summary: "Propose form: the Pointer (external git) tab is now first and selected by default for a new proposal (Hosted upload is the second tab)." },
  { version: "1.60.0", date: "2026-06-17", summary: "Signing out now clears all of skilly's auth cookies from your browser — including the httpOnly CSRF and any leftover login cookies that a normal sign-out leaves behind — then returns you to the public home." },
  { version: "1.59.2", date: "2026-06-17", summary: "Clicking Install now auto-copies the generated command to your clipboard with an “✓ Install command copied” toast; clicking the command field shows the same toast." },
  { version: "1.59.1", date: "2026-06-17", summary: "New users now default to the light theme instead of following the operating system's dark-mode setting. Switching to dark via the toggle still sticks." },
  { version: "1.59.0", date: "2026-06-17", summary: "Install command hygiene: on a skill's detail page, changing the version or expiry now hides the previously generated command (and its “pinned … · expires …” caption) until you click Install again, since that command no longer matches your selection. And generating a new command now deletes your earlier unclaimed install tokens for that skill (ones never used by npx skills), so stale valid keys don't accumulate." },
  { version: "1.58.4", date: "2026-06-17", summary: "Propose form: the source panel now renders directly under its tab — the SKILL.md bundle upload in the Hosted tab and the install-command/git fields in the Pointer tab — instead of at the bottom of the form, with the descriptive fields following." },
  { version: "1.58.3", date: "2026-06-17", summary: "Propose form: the Hosted upload / Pointer (external git) tab strip now sits at the top of the form, and the “Paste an install command” box moved into the Pointer tab (it's pointer-specific) instead of always showing at the top." },
  { version: "1.58.2", date: "2026-06-17", summary: "Propose form: the Version field now sits on its own row below Tool / harness instead of beside it, fixing the misalignment introduced by the new tool picker." },
  { version: "1.58.1", date: "2026-06-17", summary: "Header search now shows a “Nothing found” bubble when a query matches no skills, so it's clear the search actually ran (it appears only after the lookup returns empty, not while you're still typing)." },
  { version: "1.58.0", date: "2026-06-17", summary: "Tool/harness is now a closed, searchable picker of supported coding agents (Claude Code, Cursor, Gemini CLI, …) — pick one and the generated install command appends “--agent <slug>”; the default “Generic” adds no flag. Labels are shown everywhere (catalog, usage, facets), pasting an install command with “--agent” preselects the tool, and existing values not in the list still work (no flag) until changed." },
  { version: "1.57.1", date: "2026-06-17", summary: "Skill detail page: the heading now shows the skill's title, with the slug in smaller text just below it (previously the slug was shown as the heading)." },
  { version: "1.57.0", date: "2026-06-17", summary: "Deleting a skill now also deletes the review-discussion threads of its proposals (and their message alerts), instead of leaving them behind as broken “@null/?” conversations in the messages menu. A one-time cleanup removes any such orphaned threads that already existed, and stale proposal threads are now hidden defensively even if one slips through." },
  { version: "1.56.0", date: "2026-06-17", summary: "Administration gains a “Currently online” section (platform admins only): the users active in the last 5 minutes, shown as maintainer-style cards with a “Reach out” direct-message button. It has a live count, a name/email search box, and infinite scroll (100 at a time), and refreshes every 60s. Presence is derived from a new per-user last-seen timestamp stamped on authenticated activity." },
  { version: "1.55.1", date: "2026-06-17", summary: "Fixed a hosted skill that installed as empty (“No skills found” even though it published): if repo synthesis was interrupted after creating the bare repo but before writing the version tag (e.g. a brief object-storage outage mid-sweep), the half-created empty repo was treated as “provisioned” and never healed. A repo now counts as provisioned only when it actually has a version tag — empty repos are re-synthesized by the self-heal sweep, and the git server returns a clear “not provisioned” instead of serving an empty clone." },
  { version: "1.55.0", date: "2026-06-16", summary: "Pointer (external-git) proposals are now verified at submit time: if the repo/ref/folder doesn't actually contain a SKILL.md, the proposal is rejected immediately with a clear message — instead of being accepted and then silently failing to mirror later." },
  { version: "1.54.0", date: "2026-06-16", summary: "Audit log gains a search box (action, target, namespace, actor) like the System log, and both pages now have a From/To date-range calendar filter and a “✕ clear filters” button. The audit default is unchanged (newest 100 + infinite scroll); search/dates narrow on top." },
  { version: "1.53.2", date: "2026-06-16", summary: "Maintainer cards: on mobile the role tag + Reach out now wrap to a row below the avatar/name, with the remove (✕) button vertically centered. Removing a maintainer is now limited (UI and API) to the maintainer themselves, a namespace admin, or a platform admin — a plain maintainer can only remove themselves." },
  { version: "1.53.1", date: "2026-06-16", summary: "Mobile review page: the Files browser now stacks the text-file preview below the file tree (instead of beside it, where it cropped off-screen), and the chat emoji button + picker no longer clip emoji at the bottom." },
  { version: "1.53.0", date: "2026-06-16", summary: "Duplicate detection now also covers new-version proposals: uploading a hosted bundle whose contents are byte-identical to a different existing skill is blocked with the same “already in the catalog as …” message (the skill you're versioning is excluded, so legitimately re-versioning it still works)." },
  { version: "1.52.6", date: "2026-06-16", summary: "Propose form: when a submission is blocked because the skill already exists, the page now smooth-scrolls to the “propose a new version instead” banner and pulses it, so the required next step is unmissable." },
  { version: "1.52.5", date: "2026-06-16", summary: "Propose form: you can now drag a skill bundle anywhere on the page (not just onto the upload box) — a full-page “Drop to upload” overlay appears and the file lands in the upload box (switching to Hosted if needed). Dropping a file no longer risks the browser navigating away from the page." },
  { version: "1.52.4", date: "2026-06-16", summary: "Hosted upload now fails fast and clearly when object storage is unreachable: instead of a ~15s hang and a generic 500, it returns “couldn’t store the bundle — object storage is unavailable”. (Also wired the web/worker containers to wait for object storage at startup.)" },
  { version: "1.52.3", date: "2026-06-16", summary: "Propose form: a failed hosted-bundle upload now leads with the exact validation problems (e.g. “no top-level SKILL.md”, “SKILL.md name must match the slug”, “disallowed file type”, “exceeds size limit”) instead of a generic “Upload/validation failed”." },
  { version: "1.52.2", date: "2026-06-16", summary: "Propose form: uploading a bundle now pre-fills an empty Skill slug from the filename (extension dropped, slugified — “case-study-creator.skill” → “case-study-creator”) and an empty Title from that slug. Neither overwrites a value you've already entered." },
  { version: "1.52.1", date: "2026-06-16", summary: "Propose form: leaving the Skill slug field auto-fills an empty Title from the slug (separators → spaces, each word capitalized — e.g. “case-study-creator” → “Case Study Creator”). It never overwrites a title you've already typed." },
  { version: "1.52.0", date: "2026-06-16", summary: "Admins can now set the maximum hosted-skill upload size (Administration → Maximum upload size: 100 KB / 1 MB / 10 MB / 100 MB, default 10 MB). The limit is shown under the upload box on the propose form, and a bundle larger than it is rejected with “the bundle is bigger than the allowed size of X MB”." },
  { version: "1.51.4", date: "2026-06-16", summary: "Bundle upload is now resilient: the duplicate-detection pre-check is advisory and can no longer fail an otherwise-valid upload, and the upload endpoint's errors are captured in the System log so a server-side failure shows its real cause instead of a generic “Upload/validation failed”." },
  { version: "1.51.3", date: "2026-06-16", summary: "Propose form: a rejected bundle upload now explains why — the validation details (e.g. “SKILL.md name must match the skill slug”, a disallowed file type, or a size overflow) are shown instead of a bare “invalid bundle”." },
  { version: "1.51.2", date: "2026-06-16", summary: "Uploading a .skill bundle now extracts reliably: when the archive's magic bytes don't sniff cleanly, the .skill (and .zip) extension is honored and the file is read as a zip, instead of failing with “unsupported archive”." },
  { version: "1.51.1", date: "2026-06-16", summary: "Propose form: the Pinned ref field now offers an autocomplete dropdown of the upstream repo's real branches and tags — available when proposing a new version, the same as when proposing a skill for the first time." },
  { version: "1.51.0", date: "2026-06-16", summary: "Duplicate-skill detection: proposing a skill that already exists — a pointer to the same repo/folder under the same slug, or a byte-identical upload (packaging-independent) — now redirects you to propose a new version of it, carrying over the source you already provided. Admins choose hard-block or soft-warn (Administration → Duplicate proposals); reviewers are alerted on the review page either way." },
  { version: "1.50.4", date: "2026-06-15", summary: "Sidebar nav: “Propose a skill” now sits above “Review queue”." },
  { version: "1.50.3", date: "2026-06-15", summary: "When a user has neither a display name nor an email on record, messages and discussions now show “Unknown user” instead of a blank or a raw object id." },
  { version: "1.50.2", date: "2026-06-15", summary: "Maintainer names/search no longer fall back to Entra object ids: the maintainers list and picker now show a user's email when their name is missing, the search matches name+email, and reconciliation no longer overwrites a user's email with a blank when Graph omits it (same protection the display name already had)." },
  { version: "1.50.1", date: "2026-06-15", summary: "Fixed users showing as their Entra object id instead of their name in messages/discussions: reconciliation no longer falls back to the object id (or overwrites a real name with a blank), and the messaging UI now shows a user's email rather than a raw id whenever a name is missing." },
  { version: "1.50.0", date: "2026-06-15", summary: "The topbar messages dropdown now loads the 100 most recent conversations and fetches older ones with infinite scroll; the unread badge counts all conversations regardless of how many are loaded." },
  { version: "1.49.7", date: "2026-06-15", summary: "Admin: saving a namespace's Maintainer contact now shows a “✓ Saved” confirmation (and keeps the field in sync) so it's clear the change took — it was saving silently before." },
  { version: "1.49.6", date: "2026-06-15", summary: "Signed-out visitors to a members-only page (catalog, propose, etc.) are now sent to the public landing instead of seeing a Sign in button in the page body; sign-in stays in the sidebar." },
  { version: "1.49.5", date: "2026-06-15", summary: "A self-thread (reaching out to yourself, used for dev testing) now shows your own name and avatar in the messages list and header instead of a bare “Direct message”." },
  { version: "1.49.4", date: "2026-06-15", summary: "The messages dropdown's conversation list now shows each direct message's other-person avatar bubble and name (not just in the open thread)." },
  { version: "1.49.3", date: "2026-06-15", summary: "A direct-message thread now shows who you're talking to — their avatar bubble and name in the header — instead of a generic “Direct message” label." },
  { version: "1.49.2", date: "2026-06-15", summary: "Dev-only: under passwordless dev sign-in, the maintainer “Reach out” button also appears on your own card (opening a self-thread) so a solo developer can exercise messaging without a second account. No effect in production." },
  { version: "1.49.1", date: "2026-06-15", summary: "Signed-out pages no longer show the wordy “Sign in required — this page is available to signed-in members” wall; they show just a quiet Sign in button." },
  { version: "1.49.0", date: "2026-06-15", summary: "Review page: a Files browser for uploaded skill bundles — see the whole directory tree, read any text file inline, and download the rest, so reviewers can inspect every file before approving (pointer skills still link out to their upstream repo)." },
  { version: "1.48.0", date: "2026-06-15", summary: "System log alerts: platform admins now get a coalesced notification when new error events appear, and the System log sidebar link shows a 1–9+ badge of events since they last opened it (cleared on visit)." },
  { version: "1.47.1", date: "2026-06-15", summary: "System log now reliably captures server errors that routes throw (not just the errors they return), recording them in the route's own context with the actual message — and the Usage and Leaderboard endpoints are now covered, so 500s there show up in the log." },
  { version: "1.47.0", date: "2026-06-15", summary: "New System log (platform admins only, under Audit log): the HTTP errors the platform returned to users — server faults plus the meaningful 403/409/422/429 — with who hit them, a search bar, status filters, and infinite scroll. 401s and API 404 polling noise are excluded; events are kept 90 days." },
  { version: "1.46.1", date: "2026-06-15", summary: "Unread messages and notifications now glow in the theme accent — the topbar badges and the per-item unread dots — so they catch the eye (respects reduced-motion)." },
  { version: "1.46.0", date: "2026-06-15", summary: "Messages menu now shows each thread's last-message time and sorts newest-first; expanding a skill on the Usage page animates open; and the Usage skill list loads 100 at a time with infinite scroll (no longer capped at 100)." },
  { version: "1.45.0", date: "2026-06-15", summary: "Chart range memory: the 7d/30d/90d/All choice is now remembered across visits — one window for the per-skill charts (Usage breakdowns and the skill-detail chart) and one for the Usage platform totals." },
  { version: "1.44.2", date: "2026-06-15", summary: "Fixed the chat emoji picker's rightmost column spilling outside its popup so every emoji now sits inside the box." },
  { version: "1.44.1", date: "2026-06-15", summary: "Mobile menu: the drawer's close button now sits in a row with the brand and pushes the 'registry' label left instead of overlapping it." },
  { version: "1.44.0", date: "2026-06-15", summary: "Reach out to a skill's maintainers: a Reach out button on each maintainer card starts a direct chat that opens in the messages menu." },
  { version: "1.43.5", date: "2026-06-15", summary: "Fixed the chat emoji button's glyph being clipped at the bottom in the composer." },
  { version: "1.43.4", date: "2026-06-15", summary: "On mobile, the messages panel now opens as a full-screen sheet with a close button instead of an anchored dropdown that could spill off-screen." },
  { version: "1.43.3", date: "2026-06-15", summary: "Added a styled close (✕) button to the mobile navigation drawer." },
  { version: "1.43.2", date: "2026-06-15", summary: "Fixed the messages icon jumping to the left on mobile and its dropdown opening off-screen — it now stays beside the bell with the chat panel on-screen." },
  { version: "1.43.1", date: "2026-06-15", summary: "Review queue now shows who proposed each skill and when, and sorts oldest-first so the longest-waiting proposal sits on top." },
  { version: "1.43.0", date: "2026-06-15", summary: "Messaging: a submitter card and a built-in chat on the review page (submitter, reviewers and maintainers), a messages icon left of the bell with an inline chat + unread badge, and message alerts in your notifications. Emoji supported." },
  { version: "1.42.0", date: "2026-06-15", summary: "Review page: the Security scan section now has an expandable Anti-virus (ClamAV) panel showing the exact per-file engine result, even when nothing is flagged." },
  { version: "1.41.1", date: "2026-06-15", summary: "Overview page now shows the key-based install command (required even for public skills) and explains expirations and pinned vs. latest versions." },
  { version: "1.41.0", date: "2026-06-15", summary: "Added this What's new page (linked in the account menu) — a chronological log of every release." },
  { version: "1.40.0", date: "2026-06-15", summary: "Unified every range filter on the Usage page to 7d / 30d / 90d / All, styled as the same single-piece toggle used by the skill-detail chart." },
  { version: "1.39.0", date: "2026-06-15", summary: "Pointer-skill proposals are now security-scanned before review, so reviewers see findings before accepting (was scanned only on publish)." },
  { version: "1.38.0", date: "2026-06-15", summary: "The per-skill chart in Usage now follows the viewers/installers window so both show the same period; fixed the tokenless-installs box border." },
  { version: "1.37.0", date: "2026-06-14", summary: "The installs & views chart on a skill page is now visible to anyone who can view the skill (named viewer/installer breakdown stays owner-only)." },
  { version: "1.36.0", date: "2026-06-14", summary: "Added an installs & views trend chart to the skill detail page, plus an all-time range on the Usage dashboard." },
  { version: "1.34.0", date: "2026-06-14", summary: "The catalog “new” tag now marks skills that are new to you since your last visit, instead of everything updated in the last 30 days." },
  { version: "1.33.4", date: "2026-06-14", summary: "Clicking Install with an unresolved expiry date now explains why no install command was generated." },
  { version: "1.33.3", date: "2026-06-14", summary: "Propose page: visibility is derived from the chosen namespace, and Categories moved directly below the title." },
  { version: "1.33.1", date: "2026-06-14", summary: "Install-command generation is blocked until an expiry date (or “Never”) is chosen." },
  { version: "1.33.0", date: "2026-06-14", summary: "Upgraded to the latest stable frameworks: Next 16, React 19, Express 5, and TypeScript 6." },
  { version: "1.32.0", date: "2026-06-14", summary: "Database workflow optimizations: a single-round-trip install recorder and new foreign-key indexes." },
  { version: "1.31.0", date: "2026-06-14", summary: "Reusable, revocable install tokens and a new Installed Skills page; removed the old CI/PAT tokens." },
  { version: "1.30.1", date: "2026-06-14", summary: "Fixed the leaderboard opt-in not being reflected in both the 30-day and all-time windows." },
  { version: "1.30.0", date: "2026-06-14", summary: "The propose form pre-checks the pinned pointer ref against the upstream repo and offers a quick-pick of real refs." },
  { version: "1.29.0", date: "2026-06-14", summary: "Per-page Enter shortcuts, and the catalog now remembers your view, filters, and sort across visits." },
  { version: "1.27.0", date: "2026-06-14", summary: "Indexed the most-frequent previously-unindexed queries for faster hot paths." },
  { version: "1.26.0", date: "2026-06-13", summary: "Performance pass: more indexes, RBAC/response caching, and client bundle splitting." },
  { version: "1.25.0", date: "2026-06-13", summary: "Added a sidebar scroll hint and a star-framed “new” tag." },
  { version: "1.24.0", date: "2026-06-13", summary: "Pinned the “new” tag to the card/row edge and made submit errors scroll into view and flash." },
  { version: "1.23.0", date: "2026-06-13", summary: "Added a catalog “new” badge for recently updated skills." },
  { version: "1.22.0", date: "2026-06-13", summary: "Nav badges showing new-since-last-viewed counts on Catalog and the Review queue." },
  { version: "1.21.1", date: "2026-06-13", summary: "Matched the profile leaderboard toggle to the date-format toggle styling." },
  { version: "1.21.0", date: "2026-06-13", summary: "Added a Profile page, audit-log trimming, and a contributor leaderboard." },
  { version: "1.18.0", date: "2026-06-13", summary: "Capped notification history at the latest 1000 per user." },
  { version: "1.17.0", date: "2026-06-13", summary: "Platform admins can now permanently delete an archived skill and all its data." },
  { version: "1.16.0", date: "2026-06-13", summary: "Multi-CLI install-command parsing, an animated paste hint, and a logged-out navigation experience." },
  { version: "1.15.4", date: "2026-06-13", summary: "The namespace selector now lists the global namespace first." },
  { version: "1.15.3", date: "2026-06-13", summary: "Propose form: the namespace is now a themed select, matching the visibility control." },
  { version: "1.15.2", date: "2026-06-13", summary: "Stricter namespace picker and a themed visibility select on the propose form." },
  { version: "1.15.0", date: "2026-06-13", summary: "Propose form: drag-and-drop bundle upload, source tabs, and a namespace combobox." },
  { version: "1.13.3", date: "2026-06-12", summary: "Blocked restricted-visibility skills from living in the global namespace." },
  { version: "1.13.1", date: "2026-06-12", summary: "Added a Share button to the skill detail page." },
  { version: "1.13.0", date: "2026-06-12", summary: "Notifications now show skill names, and added an EU/US date-format setting." },
  { version: "1.12.0", date: "2026-06-12", summary: "Catalog, usage, and notifications UX batch, plus watcher counts." },
  { version: "1.7.1", date: "2026-06-12", summary: "Pointer mirror resilience, a catalog source filter, and review-queue & markdown UX improvements." },
  { version: "1.4.0", date: "2026-06-12", summary: "Search over usage examples, markdown for description/usage, pending-mirror status, and theming." },
  { version: "1.0.0", date: "2026-06-12", summary: "Initial release: governed SKILL.md registry with propose/review/publish, the authenticated git install gateway, Entra identity, and maintainer avatars." },
];
